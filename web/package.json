{
  "name": "sensing-pondy-web",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Field capture PWA: on-device aquatic plant identification, Tamil-first",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "cf:login": "wrangler login",
    "cf:whoami": "wrangler whoami",
    "deploy": "npm run build && wrangler pages deploy dist --project-name=sensing-ponds --branch=main",
    "deploy:preview": "npm run build && wrangler pages deploy dist --project-name=sensing-ponds --branch=preview"
  },
  "dependencies": {
    "onnxruntime-web": "^1.20.1",
    "react": "^19.0.0",
    "react-dom": "^19.0.0"
  },
  "devDependencies": {
    "@vitejs/plugin-basic-ssl": "^2.3.0",
    "@vitejs/plugin-react": "^4.3.4",
    "vite": "^6.0.7",
    "vite-plugin-pwa": "^0.21.1",
    "vitest": "^2.1.8",
    "wrangler": "^4.86.0"
  }
}
