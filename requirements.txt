# Mobile field-identification classifier
# See docs/classifier-options.md#mobile-field-identification-track

torch>=2.2
torchvision>=0.17
timm>=1.0.9              # Apache-2.0; deliberately not ultralytics (AGPL-3.0)
albumentations>=1.4.21   # >=1.4.21 for the RandomResizedCrop(size=...) signature
opencv-python-headless>=4.9
numpy>=1.26
pillow>=10.2
scikit-learn>=1.4

# Mobile export. onnx for the graph, onnxconverter-common for the fp16
# conversion that ships (5.81 MB -> 2.94 MB at zero accuracy cost), onnxruntime
# to re-verify the converted graph on held-out data before it is allowed to
# ship. Training still runs without them; export degrades to fp32 only.
onnx>=1.16
onnxruntime>=1.20
onnxconverter-common>=1.14
