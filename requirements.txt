# Mobile field-identification classifier
# See docs/classifier-options.md#mobile-field-identification-track

torch>=2.2
torchvision>=0.17
timm>=1.0.9              # Apache-2.0; deliberately not ultralytics (AGPL-3.0)
albumentations>=1.4.21   # >=1.4.21 for the RandomResizedCrop(size=...) signature
opencv-python-headless>=4.9
numpy>=1.26
pillow>=10.2
scikit-learn>=1.4

# Mobile export. Needed for --export-tflite: onnx for the graph, onnxruntime for
# INT8 dynamic quantisation (5.79 MB -> 1.58 MB) and the accuracy comparison
# against fp32. Training still runs without them; export degrades to fp32 only.
onnx>=1.16
onnxruntime>=1.20
